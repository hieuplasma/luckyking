import {StyleSheet} from 'react-native';

export const Standard = StyleSheet.create({
  Border: {
    flex: 1,
    borderRadius: 10,
    borderWidth: StyleSheet.hairlineWidth,
    borderColor: 'rgb(230,231,234)',
    backgroundColor: 'white',
    marginHorizontal: 8,
    paddingHorizontal: 12,
    justifyContent: 'center',
  },
});
